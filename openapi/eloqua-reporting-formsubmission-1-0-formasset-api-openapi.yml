openapi: 3.2.0
info:
  title: REST API for Oracle Eloqua Marketing Cloud Service Reporting/Form Submission/1.0/Form Asset API
  description: The Oracle Eloqua Marketing Cloud Service REST APIs enable you to extend the functionality of the product, build Apps, and perform high volume data transfers.
  version: 2026.08.07
  x-summary: The Oracle Eloqua Marketing Cloud Service REST APIs enable you to extend the functionality of the product, build Apps, and perform high volume data transfers.
tags:
- name: Reporting/FormSubmission/1.0/FormAsset
  description: The operations from the Reporting/FormSubmission/1.0/FormAsset category.
paths:
  /API/OData/FormSubmission/1/FormAsset:
    get:
      tags:
      - Reporting/FormSubmission/1.0/FormAsset
      summary: Get all Form Assets
      operationId: FormAsset_Get
      parameters:
      - name: $select
        in: query
        description: Limits the properties returned in the result.
        required: false
        schema:
          type: string
      - name: $expand
        in: query
        description: Indicates the related entities to be represented inline. The maximum depth is 2.
        required: false
        schema:
          type: string
      - name: $filter
        in: query
        description: 'Restricts the set of items returned. The maximum number of expressions is 100. The allowed functions are: allfunctions.'
        required: false
        schema:
          type: string
      - name: $orderby
        in: query
        description: Specifies the order in which items are returned. The maximum number of expressions is 5.
        required: false
        schema:
          type: string
      - name: $top
        in: query
        description: Limits the number of items returned from a collection. The maximum value is 20000.
        required: false
        schema:
          type: integer
          format: int32
      - name: $skip
        in: query
        description: Excludes the specified number of items of the queried collection from the result.
        required: false
        schema:
          type: integer
          format: int32
      - name: $count
        in: query
        description: Indicates whether the total count of items within a collection are returned in the result.
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: FormAssets successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FormAsset'
            application/xml:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FormAsset'
      x-internal-id: API-OData-FormSubmission-1-FormAsset-get
      x-filename-id: api-odata-formsubmission-1-formasset-get
  /API/OData/FormSubmission/1/FormAsset/{key}:
    get:
      tags:
      - Reporting/FormSubmission/1.0/FormAsset
      summary: Get a single Form Asset
      operationId: FormAsset_Get_By_ID
      parameters:
      - name: key
        in: path
        description: FormAssetID
        required: true
        schema:
          type: integer
          format: int32
      - name: $select
        in: query
        description: Limits the properties returned in the result.
        required: false
        schema:
          type: string
      - name: $expand
        in: query
        description: Indicates the related entities to be represented inline. The maximum depth is 2.
        required: false
        schema:
          type: string
      - name: $filter
        in: query
        description: 'Restricts the set of items returned. The maximum number of expressions is 100. The allowed functions are: allfunctions.'
        required: false
        schema:
          type: string
      - name: $orderby
        in: query
        description: Specifies the order in which items are returned. The maximum number of expressions is 5.
        required: false
        schema:
          type: string
      - name: $top
        in: query
        description: Limits the number of items returned from a collection. The maximum value is 20000.
        required: false
        schema:
          type: integer
          format: int32
      - name: $skip
        in: query
        description: Excludes the specified number of items of the queried collection from the result.
        required: false
        schema:
          type: integer
          format: int32
      - name: $count
        in: query
        description: Indicates whether the total count of items within a collection are returned in the result.
        required: false
        schema:
          type: boolean
          default: false
      responses:
        '200':
          description: FormAssets were successfully retrieved
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FormAsset'
            application/xml:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FormAsset'
      x-internal-id: API-OData-FormSubmission-1-FormAsset-{key}-get
      x-filename-id: api-odata-formsubmission-1-formasset-key-get
components:
  schemas:
    FormAsset:
      type: object
      properties:
        formID:
          format: int32
          type: integer
        formName:
          type: string
        isDeleted:
          type: boolean
        isArchived:
          type: boolean
        formExpired:
          type: boolean
        formHostType:
          type: string
        lastModifiedDate:
          format: date-time
          type: string
