naftiko: 1.0.0-alpha2
info:
  label: Oracle Eloqua Bulk API — Contact Fields
  description: 'Oracle Eloqua Bulk API — Contact Fields. 1 operations. Lead operation: Oracle Eloqua List contact fields.
    Self-contained Naftiko capability covering one Eloqua business surface.'
  tags:
  - Eloqua
  - Contact Fields
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ELOQUA_API_KEY: ELOQUA_API_KEY
capability:
  consumes:
  - type: http
    namespace: bulk-contact-fields
    baseUri: https://secure.p01.eloqua.com/API/Bulk/2.0
    description: Oracle Eloqua Bulk API — Contact Fields business capability. Self-contained, no shared references.
    resources:
    - name: contacts-fields
      path: /contacts/fields
      operations:
      - name: listcontactfields
        method: GET
        description: Oracle Eloqua List contact fields
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.ELOQUA_API_KEY}}'
  exposes:
  - type: rest
    namespace: bulk-contact-fields-rest
    port: 8080
    description: REST adapter for Oracle Eloqua Bulk API — Contact Fields. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/contacts/fields
      name: contacts-fields
      description: REST surface for contacts-fields.
      operations:
      - method: GET
        name: listcontactfields
        description: Oracle Eloqua List contact fields
        call: bulk-contact-fields.listcontactfields
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: bulk-contact-fields-mcp
    port: 9090
    transport: http
    description: MCP adapter for Oracle Eloqua Bulk API — Contact Fields. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: oracle-eloqua-list-contact-fields
      description: Oracle Eloqua List contact fields
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: bulk-contact-fields.listcontactfields
      outputParameters:
      - type: object
        mapping: $.
